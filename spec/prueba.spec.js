/* describe("demo", function () {
  it("Este test debe pasar siempre", function () {
    expect(4 + 2).toBe(6);
  });
}); */

/* const { Activity, Repository } = require("../scripts/index");

describe("Activity", function() {
  it("Debe estar definido el constructor", function(){
    expect(Activity.prototype.constructor).toBeDefined();
  });

  it("Debe crear una actividad con valores dados por argumento", function() {
    const activity = new Activity("Título", "Descripción", "imagen.jpg");
    expect(activity.title).toBe("Título");
    expect(activity.description).toBe("Descripción");
    expect(activity.imgUrl).toBe("imagen.jpg");
  });
});

describe("Repository", function() {
  let repository = new Repository;
  it("Debe inicializar con un array de actividades vacío", function() {
    expect(repository.activities.length).toBe(0);
  });
}); */

//No logré hacer correr los test y que funcione la página
//Tengo el error: Error during loading: require is not defined in http://localhost:8888/__spec__/prueba.spec.js line 7
